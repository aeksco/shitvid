{
  "name": "shitvid",
  "version": "0.1.0",
  "description": "Make your videos look shitty with compression artifacts",
  "author": "Alexander Schwartzberg <aeksco@gmail.com>",
  "private": true,
  "scripts": {
    "dev": "tsc -w -p tsconfig.build.json",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "chalk": "^2.4.1",
    "commander": "^2.19.0",
    "inquirer": "^6.2.0",
    "semver": "^5.6.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/node": "^10.10.0",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "typescript": "^3.7.4"
  }
}
